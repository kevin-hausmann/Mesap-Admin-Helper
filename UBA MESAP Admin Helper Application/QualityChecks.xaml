<UserControl xaml:Class="UBA.Mesap.AdminHelper.QualityChecks"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:xaml="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <ItemsControl Name="_QualityCheckList" DockPanel.Dock="Right">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25*" />
                            <RowDefinition Height="10" />
                        </Grid.RowDefinitions>
                        <CheckBox IsChecked="False" Click="SelectCheck" Tag="{Binding Id}" Margin="5" ToolTip="{Binding Description}">
                            <TextBlock Text="{Binding Name}" />
                        </CheckBox>
                        <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="{Binding Completion, Mode=OneWay}" Margin="2" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Label Name="_FilterCountLabel" Margin="5">
                Verwenden Sie eine Sicht mit der ID "QualityCheck" um den Filter zu setzen.
            </Label>
            <Button Name="_RunQualityChecks" Margin="5" Click="RunQualityChecks" IsEnabled="False">
                Prüfungen durchführen!
            </Button>
            <Button Name="_CancelQualityChecks" Margin="5" Click="CancelQualityChecks" IsEnabled="False">
                Alle abbrechen!
            </Button>
        </StackPanel>
        <ListView Name="_ResultListView">
            <ListView.View>
                <GridView ColumnHeaderTemplate="{StaticResource Header}">
                    <GridViewColumn Header="Check" DisplayMemberBinding="{Binding Path=CheckId}" />
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Path=Title}" />
                    <GridViewColumn Header="Description" />
                    <GridViewColumn Header="Category" />
                    <GridViewColumn Header="Priority" />
                    <GridViewColumn Header="ContactPerson" />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
